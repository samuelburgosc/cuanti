# Polish Pass — Accessibility & Theming
> Fecha: 2026-04-14
> Basado en el audit (13/20) — objetivo: subir a 17+/20

## Contexto
La app pasó de dark mode a light mode. Los tokens actuales son:
- `--bg:#F5F5F2` `--sf:#FFFFFF` `--sf2:#F2F2EE` `--sf3:#EAEAE6`
- `--bd:#E6E3DD` `--bd2:#D4D0CA` `--text:#171717` `--muted:#7A7A7A`
- `--accent:#D7F02E` `--red:#D84B4B` `--green:#1E9E63`

## Fixes a realizar

### P1 — Touch targets demasiado pequeños
- [x] `.icon-btn` y `.avatar-btn`: 32px → 40px (línea ~236-239 CSS)
- [x] `.ni` nav items: agregar `min-height:44px` (línea ~591)
- [x] `.modal-close` y `.qty-ctrl button`: 28px → 36px (línea ~631-647)

### P1 — Aria labels en botones de ícono
- [x] Campana de notificaciones → `aria-label="Notificaciones"`
- [x] Avatar btn → `aria-label="Mi perfil"`
- [x] Escáner → `aria-label="Escáner"`
- [x] `role="button"` + `tabindex="0"` en `.ni` nav items

### P1 — Focus visible en elementos interactivos
- [x] Agregar reglas `:focus-visible` para divs interactivos

### P2 — Hardcoded colors
- [x] `.topbar` background/border → `var(--sf)` / `var(--bd)`
- [x] `.logo` color → `var(--text)`
- [x] `rgba(255,68,68,.12)` en renderGastos() → `var(--rdim)`

### P3 — Blur y documentación
- [x] `backdrop-filter: blur(20px)` → `blur(8px)` en bottom-nav
- [x] Actualizar CLAUDE.md con tokens reales (light mode)

## Commits realizados
1. `polish: touch targets, aria-labels, focus visible`
2. `polish: hardcoded colors → tokens`
3. `polish: blur reducido, docs actualizadas`

## Estado
- [x] **COMPLETO** — todos los fixes aplicados
